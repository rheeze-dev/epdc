@model src.Models.Organization
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Audit Trail";
    ApplicationUser appUser = await UserManager.GetUserAsync(User);
    Layout = "_LayoutAdminlteAdmin";
}
    <style>
        .btn-primary {
            background-color: #3c8dbc !important;
            border-color: #367fa9 !important;
        }

        .btn-default:active, .btn-default.active, .open > .dropdown-toggle.btn-default {
            color: #333 !important;
            background-color: #3c8dbc !important;
            border-color: #adadad !important;
        }

        .btn-lg, .btn-group-lg > .btn {
            padding: 10px 16px !important;
            font-size: 14px !important;
            line-height: 1.8 !important;
            border-radius: 6px !important;
        }

        .audit {
            border-left: 3px solid #3c8dbc !important;
            color: white !important;
            background: #1e282c !important;
        }

        .skin-blue .sidebar-menu>li.active>a {
    border-left-color: rgba(0,0,0,0.1) !important;
}
.skin-blue .sidebar-menu>li:hover>a, .skin-blue .sidebar-menu>li.active>a, .skin-blue .sidebar-menu>li.menu-open>a {
    color: #b8c7ce !important;
    background: #1e282c; 
}
    </style>
@*<style>
    .btn-primary {
    background-color: #3c8dbc;
    border-color: #367fa9;
}

.btn-default:active, .btn-default.active, .open > .dropdown-toggle.btn-default {
    color: #333;
    background-color: #3c8dbc;
    border-color: #adadad;
}

    .btn-lg, .btn-group-lg > .btn {
    padding: 10px 16px;
    font-size: 14px;
    line-height: 1.8;
    border-radius: 6px;
}
</style>*@
<!-- Content Header (Page header) -->
<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-lg-12">
            <div class="box box-primary">
                <div class="box-body">
                    <div class="row">
                        <div class="col-lg-12">
                            @*<div class="col-md-6">
            <a class="btn btn-success" style="margin-bottom:10px" onclick="ShowPopup('@Url.Action("AddEditIndex","Members")')"><i class="fa fa-plus"></i> New Personnel</a>
        </div>*@
                            <a class="header btn-default btn-lg @Html.IsSelected(controller: "AuditTrail", action: "Index")" href="@Url.Action("Index","AuditTrail")">Edited data</a> &nbsp;
                            <a class="header btn-default btn-lg @Html.IsSelected(controller: "AuditTrail", action: "DeletedDatas")" href="@Url.Action("DeletedDatas","AuditTrail")">Deleted data</a>

                            <input type="hidden" asp-for="@Model.organizationId" />
                            <table id="grid" name="grid" class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th class="col-md-1">Datetime edited</th>
                                        <th class="col-md-1">Control number</th>
                                        <th class="col-md-1">From</th>
                                        <th class="col-md-1">Original datas</th>
                                        <th class="col-md-1">Edited by</th>
                                        <th class="col-md-1">Remarks</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                    </div>
                </div>

            </div>
        </div>

    </div>

</section>

<!-- /.content -->
<!--modal placeholder-->
@*<div class="modal modal-primary fade" id="modalDefault">
        <div class="modal-dialog">
            <div class="modal-content">

            </div>
        </div>
    </div>*@



@section Scripts{
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script src="~/js/views/auditTrail/index.js"></script>
}

